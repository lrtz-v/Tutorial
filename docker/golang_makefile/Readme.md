
# Commands

```bash
# test & lint
make test

# build
make build

# run
make run

# deps
make deps

# build docker image
make docker

# clean
make clean

# run image
docker run --rm -it -e url=http://www.bing.com go-makefile-test-app:latest
```
